@model SportsManagement.Models.game

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>game</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Game_ID)

        @Html.HiddenFor(model => model.Game_Code)

        @Html.HiddenFor(model => model.Game_Name)

        @Html.HiddenFor(model => model.Game_DurationInHours)

        @Html.HiddenFor(model => model.Game_Description)

        @Html.HiddenFor(model => model.Game_RulesBooklet)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
